# Private preview — HTTP Basic Auth baked into the repo so protection survives
# every deploy (independent of hPanel Directory Privacy). Remove this block at
# public launch. AuthUserFile is relative to this .htaccess (LiteSpeed/Hostinger).
AuthType Basic
AuthName "Wildcard - Private Preview"
AuthUserFile ".htpasswd"
Require valid-user

ErrorDocument 404 /404.html
